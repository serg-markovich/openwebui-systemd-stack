[Unit]
Description=Open WebUI Stack - systemd managed

[Service]
Type=oneshot
RemainAfterExit=yes
# Use %h (home) for portability
WorkingDirectory=%h/FOUNDATION/projects/privacy/openwebui-stack
ExecStart=/usr/bin/docker compose up -d
ExecStop=/usr/bin/docker compose down
StandardOutput=journal
StandardError=journal
Restart=no

[Install]
WantedBy=default.target
