# Open WebUI configuration
# Copy to .env and adjust if needed: cp .env.example .env

# Ollama API endpoint (Docker bridge gateway - do not change unless custom setup)
OLLAMA_BASE_URL=http://172.17.0.1:11434

# Port exposed on host (change if 3000 is busy)
WEBUI_PORT=3000
